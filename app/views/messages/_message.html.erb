<div id="message-<%= message.id %>">
  <small>
    <span class="user-nickname" slyle="color: blue;"><%= message.user.nickname %></span>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p class="m-2"><%= message.content %></p>
</div>
